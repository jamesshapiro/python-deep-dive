# Usage:
# (1.) Navigate to the directory containing the MainUsage directory
# (2.) Run the following command:
# $ python3 MainUsage

if __name__ == '__main__':
    print('Running a file from a directory...')